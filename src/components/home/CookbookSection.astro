<section class="bm-cookbook" aria-labelledby="bm-cookbook-title">
  <div class="container-site bm-cookbook__grid">
    <div class="bm-cookbook__visual">
      <div class="bm-cookbook__halo" aria-hidden="true"></div>
      <div class="bm-cookbook__scene" aria-hidden="true">
        <span class="bm-cookbook__linen"></span>
        <span class="bm-cookbook__branch"></span>
        <span class="bm-cookbook__olive bm-cookbook__olive--1"></span><span class="bm-cookbook__olive bm-cookbook__olive--2"></span><span class="bm-cookbook__olive bm-cookbook__olive--3"></span>
      </div>
      <div class="bm-book" aria-label="Biblical Meals cookbook cover mockup">
        <div class="bm-book__spine">BIBLICAL MEALS</div>
        <div class="bm-book__cover">
          <p>A BiblicalMeal Cookbook</p>
          <h3>BIBLICAL<br />MEALS</h3>
          <span></span>
          <p>RECIPES INSPIRED BY<br />THE FOODS OF THE BIBLE</p>
          <small>Digital cookbook · in development</small>
        </div>
        <div class="bm-book__pages"></div>
        <div class="bm-book__page-shadow"></div>
      </div>
    </div>

    <div class="bm-cookbook__copy">
      <p class="bm-eyebrow">The Cookbook</p>
      <h2 id="bm-cookbook-title">Biblical<br />Meals<span>.</span></h2>
      <p class="bm-cookbook__lead">A collection of recipes, stories, and tables inspired by the foods of the Bible.</p>
      <div class="bm-cookbook__points">
        <article><b>01</b><h3>Recipes inspired by Scripture</h3></article>
        <article><b>02</b><h3>Stories behind the food</h3></article>
        <article><b>03</b><h3>Made for the modern kitchen</h3></article>
      </div>
      <a class="bm-btn bm-btn--gold" href="/cookbook/">Discover the cookbook <span aria-hidden="true">→</span></a>
    </div>
  </div>
</section>

<style>
  .bm-cookbook {
    position: relative;
    overflow: hidden;
    padding-block: clamp(110px, 10vw, 146px);
    background: #14200f;
    color: #f3ebdd;
  }
  .bm-cookbook::after {
    content: '';
    position: absolute;
    right: -160px;
    bottom: -260px;
    width: 520px;
    height: 520px;
    border: 1px solid rgba(199, 171, 114, 0.1);
    border-radius: 50%;
    box-shadow: 0 0 0 60px rgba(199, 171, 114, 0.025), 0 0 0 120px rgba(199, 171, 114, 0.018);
    pointer-events: none;
  }
  .bm-cookbook__grid {
    position: relative;
    z-index: 1;
    display: grid;
    grid-template-columns: 1.02fr 0.98fr;
    align-items: center;
    gap: clamp(48px, 8vw, 118px);
    min-height: 600px;
  }
  .bm-cookbook__visual {
    position: relative;
    min-height: 600px;
    display: grid;
    place-items: center;
  }
  .bm-cookbook__halo {
    position: absolute;
    width: 480px;
    height: 480px;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(188, 150, 79, 0.14), transparent 67%);
  }
  .bm-cookbook__scene {
    position: absolute;
    inset: auto auto 64px 7%;
    width: 440px;
    height: 120px;
    border-radius: 50%;
    background: rgba(22, 17, 10, 0.78);
    filter: blur(1px);
  }
  .bm-cookbook__linen {
    position: absolute;
    right: 46px;
    bottom: 15px;
    width: 280px;
    height: 58px;
    border-radius: 52% 48% 44% 56%;
    background: linear-gradient(160deg, rgba(216, 203, 177, 0.24), rgba(121, 108, 87, 0.08));
    transform: rotate(6deg);
  }
  .bm-cookbook__branch {
    position: absolute;
    left: 18px;
    bottom: 48px;
    width: 170px;
    height: 38px;
    border-top: 2px solid rgba(89, 103, 63, 0.7);
    transform: rotate(-15deg);
  }
  .bm-cookbook__olive {
    position: absolute;
    width: 19px;
    height: 19px;
    border-radius: 50%;
    background: #4e5d2d;
    box-shadow: inset -3px -4px 0 rgba(0, 0, 0, 0.16);
  }
  .bm-cookbook__olive--1 { left: 36px; top: 20px; }
  .bm-cookbook__olive--2 { left: 82px; top: 50px; background: #746238; }
  .bm-cookbook__olive--3 { left: 122px; top: 23px; }
  .bm-book {
    position: relative;
    width: min(390px, 74%);
    aspect-ratio: 0.74;
    transform: perspective(1200px) rotateY(-8deg) rotateZ(-2deg);
    filter: drop-shadow(28px 34px 24px rgba(0, 0, 0, 0.44));
  }
  .bm-book__cover {
    position: absolute;
    inset: 0 20px 0 9px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 42px 30px;
    background:
      linear-gradient(140deg, rgba(78, 99, 55, 0.38), rgba(17, 28, 12, 0.18)),
      #1b2913;
    border: 1px solid rgba(199, 171, 114, 0.48);
    box-shadow: inset 0 0 0 1px rgba(199, 171, 114, 0.18), inset 14px 0 28px rgba(0, 0, 0, 0.14);
    text-align: center;
  }
  .bm-book__cover::before {
    content: '';
    position: absolute;
    inset: 11px;
    border: 1px solid rgba(199, 171, 114, 0.36);
    pointer-events: none;
  }
  .bm-book__cover > p:first-child {
    margin: 0 0 62px;
    color: #c7ab72;
    font-family: var(--font-sans);
    font-size: 9px;
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }
  .bm-book__cover h3 {
    margin: 0;
    color: #d9bd7d;
    font-family: var(--font-display);
    font-size: 44px;
    font-weight: 500;
    line-height: 0.95;
    letter-spacing: 0.08em;
  }
  .bm-book__cover > span {
    width: 62px;
    height: 1px;
    margin: 26px 0 20px;
    background: #b89653;
  }
  .bm-book__cover p:nth-of-type(2) {
    margin: 0;
    color: #c7ab72;
    font-family: var(--font-display);
    font-size: 12px;
    line-height: 1.5;
    letter-spacing: 0.06em;
  }
  .bm-book__cover small {
    margin-top: auto;
    color: rgba(243, 235, 221, 0.44);
    font-family: var(--font-sans);
    font-size: 9px;
  }
  .bm-book__spine {
    position: absolute;
    left: -4px;
    top: 7px;
    bottom: 6px;
    width: 17px;
    display: flex;
    align-items: center;
    justify-content: center;
    writing-mode: vertical-rl;
    transform: rotate(180deg);
    color: #b89653;
    background: #26361c;
    border-left: 1px solid rgba(199, 171, 114, 0.28);
    border-right: 1px solid rgba(0, 0, 0, 0.2);
    font-family: var(--font-sans);
    font-size: 8px;
    letter-spacing: 0.12em;
  }
  .bm-book__pages {
    position: absolute;
    inset: 8px -1px 5px 21px;
    z-index: -1;
    background: repeating-linear-gradient(180deg, #d8ceb8 0 2px, #b8ad97 3px, #d8ceb8 4px);
    transform: translateX(9px);
    box-shadow: 12px 4px 10px rgba(0, 0, 0, 0.12);
  }
  .bm-book__page-shadow {
    position: absolute;
    right: -7px;
    top: 12px;
    bottom: 9px;
    width: 9px;
    z-index: -2;
    background: linear-gradient(90deg, rgba(213, 203, 181, 0), rgba(213, 203, 181, 0.32));
  }
  .bm-cookbook__copy {
    max-width: 620px;
  }
  .bm-cookbook__copy .bm-eyebrow {
    margin: 0 0 12px;
    color: #c7ab72;
  }
  .bm-cookbook h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(4rem, 6.2vw, 6.4rem);
    font-weight: 400;
    line-height: 0.88;
    letter-spacing: -0.05em;
  }
  .bm-cookbook h2 span {
    color: #a84f2c;
  }
  .bm-cookbook__lead {
    max-width: 560px;
    margin: 24px 0 0;
    color: rgba(243, 235, 221, 0.7);
    font-size: 17px;
    line-height: 1.75;
  }
  .bm-cookbook__points {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    margin: 38px 0 32px;
    border-block: 1px solid rgba(199, 171, 114, 0.2);
  }
  .bm-cookbook__points article {
    padding: 19px 15px 21px 0;
  }
  .bm-cookbook__points article + article {
    padding-left: 15px;
    border-left: 1px solid rgba(199, 171, 114, 0.2);
  }
  .bm-cookbook__points b {
    color: #c7ab72;
    font-family: var(--font-display);
    font-size: 22px;
    font-weight: 400;
  }
  .bm-cookbook__points h3 {
    margin: 9px 0 0;
    color: #f3ebdd;
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 600;
    line-height: 1.55;
    letter-spacing: 0.08em;
    text-transform: uppercase;
  }
  .bm-btn--gold {
    color: #1b1915;
    background: #b89653;
  }
  .bm-btn--gold:hover {
    background: #c7ab72;
  }
  @media (max-width: 900px) {
    .bm-cookbook {
      padding-block: 96px;
    }
    .bm-cookbook__grid {
      grid-template-columns: 1fr;
    }
    .bm-cookbook__visual {
      min-height: 520px;
    }
  }
  @media (max-width: 560px) {
    .bm-cookbook {
      padding-block: 76px;
    }
    .bm-cookbook__visual {
      min-height: 438px;
    }
    .bm-book {
      width: min(310px, 82%);
    }
    .bm-cookbook__points {
      grid-template-columns: 1fr;
    }
    .bm-cookbook__points article {
      padding: 16px 0;
    }
    .bm-cookbook__points article + article {
      padding-left: 0;
      border-left: 0;
      border-top: 1px solid rgba(199, 171, 114, 0.2);
    }
  }
</style>
