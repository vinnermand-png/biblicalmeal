<section class="bm-manifesto" aria-labelledby="bm-manifesto-title">
  <div class="container-site bm-manifesto__grid">
    <div>
      <p class="bm-eyebrow">The ancient table</p>
      <h2 id="bm-manifesto-title">The ancient<br />table is still here<span>.</span></h2>
    </div>
    <div class="bm-manifesto__copy">
      <p>In the ingredients we cook with.<br />In the stories we remember.<br />And in the tables we share.</p>
      <div class="bm-manifesto__newsletter">
        <p class="bm-manifesto__label">The BiblicalMeal Letter</p>
        <p>Stories, ingredients, and meals from the ancient table.</p>
        <a class="bm-btn bm-btn--terracotta" href="#newsletter">Join the table <span aria-hidden="true">→</span></a>
      </div>
    </div>
  </div>
</section>

<style>
  .bm-manifesto {
    position: relative;
    overflow: hidden;
    min-height: 430px;
    padding-block: clamp(105px, 10vw, 138px);
    background: #14200f;
    color: #f3ebdd;
  }
  .bm-manifesto::before {
    content: '';
    position: absolute;
    left: -80px;
    top: 50%;
    width: 300px;
    height: 1px;
    background: linear-gradient(90deg, transparent, rgba(199, 171, 114, 0.28));
  }
  .bm-manifesto::after {
    content: '';
    position: absolute;
    right: -110px;
    bottom: -220px;
    width: 430px;
    height: 430px;
    border: 1px solid rgba(199, 171, 114, 0.1);
    border-radius: 50%;
    box-shadow: 0 0 0 50px rgba(199, 171, 114, 0.025), 0 0 0 100px rgba(199, 171, 114, 0.016);
    pointer-events: none;
  }
  .bm-manifesto__grid {
    display: grid;
    grid-template-columns: 1fr 0.92fr;
    align-items: center;
    gap: clamp(60px, 9vw, 130px);
  }
  .bm-manifesto .bm-eyebrow {
    margin: 0 0 16px;
    color: #c7ab72;
  }
  .bm-manifesto h2 {
    max-width: 640px;
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(4.2rem, 7vw, 7rem);
    font-weight: 400;
    line-height: 0.88;
    letter-spacing: -0.055em;
  }
  .bm-manifesto h2 span {
    color: #a84f2c;
  }
  .bm-manifesto__copy > p {
    max-width: 480px;
    margin: 0;
    color: rgba(243, 235, 221, 0.72);
    font-family: var(--font-display);
    font-size: clamp(1.5rem, 2.1vw, 2rem);
    line-height: 1.42;
  }
  .bm-manifesto__newsletter {
    max-width: 480px;
    margin-top: 38px;
    padding-top: 22px;
    border-top: 1px solid rgba(199, 171, 114, 0.2);
  }
  .bm-manifesto__label {
    margin: 0 0 7px;
    color: #c7ab72;
    font-family: var(--font-sans);
    font-size: 11px;
    font-weight: 700;
    letter-spacing: 0.18em;
    text-transform: uppercase;
  }
  .bm-manifesto__newsletter > p:not(.bm-manifesto__label) {
    margin: 0;
    color: rgba(243, 235, 221, 0.58);
    font-size: 14px;
    line-height: 1.6;
  }
  .bm-manifesto__newsletter .bm-btn {
    margin-top: 20px;
  }
  @media (max-width: 800px) {
    .bm-manifesto {
      min-height: auto;
      padding-block: 82px;
    }
    .bm-manifesto__grid {
      grid-template-columns: 1fr;
      gap: 48px;
    }
    .bm-manifesto h2 {
      font-size: clamp(3.7rem, 14vw, 5.4rem);
    }
    .bm-manifesto__copy > p {
      font-size: 1.45rem;
    }
  }
</style>
